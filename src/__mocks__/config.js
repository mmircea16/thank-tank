const config = jest.genMockFromModule('../config');
export default {
    gridSize: 1,
    minRefreshRate: 1,
    mapping: {
        name: 2,
        from: 4,
        message: 3,
        secret: 4
    }
};